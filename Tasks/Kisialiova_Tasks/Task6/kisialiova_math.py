from decimal import Decimal


def addition(a, b):
    return Decimal(a) + Decimal(b)


def subtraction(a, b):
    return Decimal(a) - Decimal(b)


def multiplication(a, b):
    return Decimal(a) * Decimal(b)


def division(a, b):
    return Decimal(a) / Decimal(b)
